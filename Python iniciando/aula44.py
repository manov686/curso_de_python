##
# Range é uma função que gera uma sequência de números, começando do 0 por padrão, e incrementando de 1 (por padrão) até um número especificado. A função range() é muito útil para criar loops que precisam iterar um número específico de vezes.

numeros = range(0, 11, 2)

for numero in numeros:
    print(numero)